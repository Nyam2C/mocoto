import React, { useState, useEffect } from "react";

interface HelloProps {
    isExpanded: boolean;
}

const Exchange: React.FC = () => {

    return (
        <div>
            거래소소거래소소거래소소거래소소거래소소거래소소거래소소거래소소거래소소거래소소거래소소
        </div>
    );
};

export default Exchange;