import React, { useState, useEffect } from "react";

interface HelloProps {
    isExpanded: boolean;
}

const Account: React.FC = () => {

    return (
        <div>
            계좌좌계좌좌계좌좌계좌좌계좌좌계좌좌계좌좌계좌좌계좌좌계좌좌계좌좌
        </div>
    );
};

export default Account;